# V-041 菌翼蛾：可玩候选接入

用户已明确要求「下载菌翼蛾并继续接入」，此前的下载限制已解除。本轮只下载现有生成结果，没有新生成、充值、购买或订阅更改。

## 可追溯资产

- 原始 PBR 与 shaded GLB：`ArtSource/Moth/Original/`，原文件不修改。
- 生成 ID：`d466e95b-fa06-435a-abbf-17abebafe4cf`。
- 来源页面：https://hyper3d.ai/workspace/rodin/d466e95b-fa06-435a-abbf-17abebafe4cf 。下载时间、文件长度及 SHA-256 在 `ArtSource/Moth/source-manifest.json`；不保存临时签名下载 URL。
- 原始网格 24,048 个三角面、12,946 个顶点、一套 UV，实际数字来自本地解析，不能将请求中的 12,000 面预算当作实际三角面数量。
- 派生文件 `ArtSource/Moth/v1/Moth_bound.blend`、`SK_Moth.fbx`、`A_Moth_Hover.fbx`、`A_Moth_Fly.fbx` 与三张 2K 贴图。
- 8 根骨骼：root、body、head、abdomen、左右前翼与后翼；翼展定为 70 厘米。保留生成 UV 与贴图，调整朝向和翼根权重。
- UE 目录 `/Game/VariantZero/Characters/Moth`。三级 LOD 顶点数 12,946 / 3,715 / 1,996；材质使用 BaseColor、OpenGL 法线转绿通道、ORM 的 AO/Roughness/Metallic 通道。

## 可玩流程

运行根目录 `打开传粉伙伴试玩.bat`。独立包 `BuildOutput-Pollinator` 保留原来的 `BuildOutput-Prologue`。

完成温室开场回访后，在培养台交互，消耗修复所得的两份研究样本，确定培育菌翼蛾。它以 V-041 物种身份及独立 GUID 加入队伍，初芽与新芽保留，队伍总计三只。没有小游戏通关要求。

带菌翼蛾回花床交互：伙伴需要健康、在花床附近，随后前往花床并停留 1.4 秒完成传粉。玩家离开、伙伴倒地、开始战斗、召回、指定新的技能落点或死亡恢复会取消未完成的传粉；可以重新尝试，不扣除额外材料。完成后同一事务保存花床状态、个体共同经历和回执，不重复结算。

当前悬停视觉复用地面导航路径、在胶囊上方抬高骨骼模型；它不具备独立三维飞行寻路，不能穿越墙体或深水。基础攻击沿用现有伙伴组件。正式物种独立战斗技能、被动、完整生态标签与花庭关卡仍未完成。

## 验证与限制

`Scripts/verify_moth.py` 重新打开保存的 Blender 文件，检查全部顶点有权重、循环首尾误差、实际顶点形变、有限坐标；再导入 FBX 检查骨骼与 UV。报告 `ArtSource/Moth/v1/verification.json`，循环误差 0，检测到约 0.102 米最大翼面运动，回导 8 骨／12,946 顶点。

UE 原生测试扩展了研究扣费、队伍、缺少传粉物种的拒绝、奖励／传粉去重与读档。`VZStoryTest` 额外检查骨架、材质、LOD、真实翼骨运动、研究、伙伴同队、离开时中断传粉、完成传粉与读档。

打包以 `pollinator-packaging.log`、`Docs/pollinator-verification.json` 为准。这是经过引擎验证的候选，不是最终美术验收；生成模型的翼缘与表面仍较简化，不能宣称已经达到计划中的写实品质。仍需保留本次账户适用授权记录后再发行；公开许可入口 https://hyper3d.ai/legal/copyright-license 。

2026-09-15 最终验证：编辑器构建成功；5 组原生测试通过；Windows 打包成功，6 项打包回归全部 PASS／退出码 0。实际打包进程另输出 `VZ_MOTH: PASS` 与 `VZ_MOTH_ANIMATION: PASS`，确认使用了新网格、材质、三级 LOD 与正在变化的翼骨。预览已检查并保存到 `Previews/零号变种-菌翼蛾接入.png`。构建路径、二进制 SHA-256 与报告位置见 `pollinator-build-manifest.json`。

传粉现阶段改变持久化花床状态与伙伴记录，尚未加入单独的果实生长模型／动画；正式花庭完成度仍以 P1 的完整内容与试玩门槛衡量。
