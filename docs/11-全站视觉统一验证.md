# 全站视觉统一 · 温室档案与遗传实验室

日期：2026-09-06

## 交付内容

- 新增 src/visual-system.css：共享自然绿、能量金、基因青、突变紫，卡片边缘高光、面板、按钮焦点及响应式约定；选择器限定档案页与弹窗，未调整棋盘布局。
- 新增 src/ui-icons.ts：本地 SVG 导航与操作图标，装饰图标对读屏隐藏，控件保留文字。
- 基地新增真实后代发现进度（9 种后代，不含三类初始母体），首次发现引导、成绩空状态与解锁标识。
- 实验室展示母体组合头像、100 阳光费用、落点说明及 60/30/10 概率条；进化与突变分色。
- 图鉴保留标本、记录展开、世界观内容；统一基因卡及纪事面板。
- 设置按声音与动态分组，音量百分比、即时保存提示、可聚焦开关；确认操作突出后果。长弹窗滚动，操作区可达。
- 无规则、经济或存档结构迁移。系统或设置减少动态效果时取消新增动画及按钮位移。

## 实际验证

Chrome 153.0.8010.12，macOS；使用独立自动化浏览器存储，不修改用户存档。

- 生产构建通过。既有 Phaser 大包提示保留（JS gzip 约 350 KB）。
- 原有浏览器回归 10/10 通过：三种尺寸下棋盘与定位、种植、阳光点击优先级、安装、进化、突变、取消、材料失效、重开/退出确认、暂停清音、过场继承、结算不重复。
- 基地、实验室、图鉴、纪事共 16 组尺寸检查：1280×720、1366×768、1440×900、390×844，无横向溢出。长档案采用纵向滚动。
- 设置在四种尺寸下 Tab 正向循环、Shift+Tab 反向循环、Esc 关闭及焦点回到设置按钮均通过。
- 音乐调整为 37%、勾选静音与减少动态效果后刷新，值正确恢复；弹窗 animation-name 为 none。
- 模拟已有两种后代与一个基因解锁，显示 2/9、1 项完成里程碑；空存档显示首次发现引导。
- Enter 展开标本记录通过；进化/突变色彩覆盖检查通过；所有页面无 JavaScript 错误。
- 人工禁用 backdrop-filter 后检查设置弹窗截图，仍以高不透明度底色显示内容；此项为降级模拟，不代表旧浏览器实测。
- Safari 26.6：尝试连接原生 UI 自动化时超时（-10005），本轮未完成 Safari 走查，不计为通过。

本地截图：.verification/unified-*；结构与状态结果：.verification/unified-results.json。截图覆盖桌面/手机四个内容页与设置，以及关闭模糊后的设置面板。
