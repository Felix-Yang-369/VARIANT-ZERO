# 零号变种：统一项目目录

当前版本：**0.2.24**。版本号以根目录 `VERSION` 为准，每次完成并交付更新默认递增小版本；详见 `版本记录.md`。

最新验证版本：跨版本存档。主线存档改为 Windows 用户目录固定位置，新增只复制、校验且不覆盖的旧档迁移。11 项原生测试、9 项打包回归通过，最终空路径保护另经原生及菜单复测。说明见 UE/Docs/跨版本存档与迁移.md；根目录新增 打开存档文件夹.bat。旧包及原始存档保留。

此前验证版本：伙伴培养与固定成长。主线解锁培养点，在研究所确认升级，实际影响生命、普攻、技能和治疗；不消耗关键主线材料。10 项原生测试、9 项打包回归通过，最终标签另经菜单复测。说明见 UE/Docs/伙伴培养与固定成长.md。旧包及存档保留。

此前验证版本：主角战斗动作。脉冲和护盾使用上半身动作，受击叠加反馈；半透明生态护罩替代调试线框。10 项原生测试、9 项打包回归通过。说明见 UE/Docs/主角战斗动作.md；实景见 UE/Previews/零号变种-主角战斗动作.png。旧包及存档保留。

此前验证版本：研究所整备。在培养台附近通过伙伴管理入队/留守并恢复队伍；地图增加常驻培养台导航。10 项原生测试、9 项打包回归通过。说明见 UE/Docs/研究所整备.md。旧包和存档保留。

此前验证版本：伙伴管理。Esc → 地图 / 研究记录 / 快速移动 → 伙伴管理，可修改名字、查看个体经历、调整技能槽顺序。10 项原生测试和 9 项打包回归通过。说明见 UE/Docs/伙伴管理.md。

此前验证版本：样章地图与导航。Esc → 地图 / 研究记录 / 快速移动 → 打开地图 / 标记目标。9 项原生测试与 9 项打包回归通过。说明：UE/Docs/样章地图与导航.md；实际截图：UE/Previews/零号变种-样章地图.png。旧包与各自存档保留，未自动迁移。

此前完成：探索音乐与受击反馈。《根系之外》已接入花庭并随战斗淡出；实际受击触发音效。8 项原生测试、9 项打包检查通过。详见 UE/Docs/探索音乐与受击反馈.md。Suno 主题曲仍受下载额度限制，未接入。

此前完成：初芽伙伴故事《第一场雨》。花庭报告后带初芽回档案台接取；玩法说明见 `UE/Docs/初芽伙伴故事-第一场雨.md`。

此前完成：声音与阅读设置。暂停菜单可独立调节音乐/音效、切换对话与档案字号；说明见 `UE/Docs/声音与阅读设置.md`。

此前完成：两阶段花庭守护者，带原创叶冠与有限破防窗口。说明见 `UE/Docs/花庭守护者两阶段.md`；实景见 `UE/Previews/零号变种-花庭守护者.png`。

日常入口：`开始游戏.bat` 打开最新验证过的开放世界原型；`打开UE工程.bat` 打开 Unreal 工程。当前内容仍非完整 1.0。

此前完成：研究记录与快速移动版。在花庭原型基础上增加暂停菜单记录回看、研究所与花庭双向移动。按 Esc 后选择「研究记录 / 快速移动」；说明见 `UE/Docs/研究记录与快速移动.md`，实际界面见 `UE/Previews/零号变种-研究记录与快速移动.png`。

`打开经典版.bat` 启动本地 v0.4 网页版并打开浏览器。首次需有 Node.js 与已安装的依赖；本次已修复 Windows 依赖入口。网页版浏览器存档与 UE 存档独立，目录整合不等于玩法移植，原生演练台仍待开发。

| 内容 | 位置 |
|---|---|
| UE 主工程、C++、场景、正式开发文档 | `UE/` |
| UE 最新试玩包 | 由 `UE/当前试玩包.txt` 指定 |
| UE 原始及派生 3D 素材 | `UE/ArtSource/` |
| UE 实际预览图 | `UE/Previews/` |
| UE 旧试玩包与各自存档 | `UE/BuildOutput*`，全部保留 |
| 原 v0.4 网页源码 | 根目录 `src/`、`public/`、`package.json` |
| 网页验证与历史资料 | 根目录 `tests/`、`docs/`、`backups/`、`output/` |
| 已整理的音乐 | `玻璃下的春天/`、`BGM-根系之外/`、`主题曲-让春天不同/` |

原 `D:\Projects\VariantZeroUE` 现为指向本目录 `UE` 的 NTFS 兼容联接，不是第二套工程。已有工具、编辑器与脚本继续访问同一份数据。请勿对兼容入口使用递归清理命令。

本机 MSVC 在中文工程路径创建预编译头时出现 C1083。构建脚本和「打开UE工程」因此使用上述英文兼容入口，输出与实际文件仍在本目录内。请保留此联接。经典版启动使用原来的 `127.0.0.1:5173`，以便在原浏览器配置中继续访问该地址的本地存档。

整合采用同盘目录移动，不重写素材；源码、配置、工程描述和现有 `.vzsave` 在移动前后逐项 SHA-256 比较。记录在 `docs/整合前校验.json` 与 `docs/整合验证.json`。没有合并或覆盖不同版本的玩家存档。

经典网页开发仍在根目录执行 `npm run dev`；构建 `npm run build`，测试 `npm test`。Vite 已排除 UE 目录的文件监听，避免监听大量引擎缓存。













