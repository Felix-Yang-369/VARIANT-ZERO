# 零号变种项目规则

## 版本号（用户指定）

- 根目录 `VERSION` 是版本号的唯一来源，格式为 `大版本.中版本.小版本`。
- 当前版本为 `0.2.24`，对应现有共享存档试玩版。按用户要求粗略追溯累计之前的更新，以现存 24 份含可执行文件的阶段试玩包作为估算依据；不是精确的历史发布次数。
- 每完成并交付一次更新，默认仅将小版本加 1：`0.2.24 → 0.2.25 → 0.2.26`。超过 9 继续递增，例如 `0.2.10`。
- 不自动改变大版本 `0` 或中版本 `2`；只有用户明确指定才调整。
- 一次更新中的多次编辑、修复、测试和重新打包共用一个版本，不因每次构建递增。查询、讨论、试玩说明及原包的再次分享不递增。
- 更新交付时，同步 `VERSION`、`UE/Config/DefaultGame.ini` 的 `ProjectVersion`、`版本记录.md` 和本次构建记录。新分享包文件名包含版本号。
- 最终回复必须明确写出本次版本号和主要变化。构建或验收未通过时，不将其称为已交付版本。
- 旧试玩包、旧分享链接及存档保留；不能仅修改元数据就声称旧二进制已经重新构建。

